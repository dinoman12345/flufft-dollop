camera=document.getElementById("camera");
Webcam.attach(camera);
Webcam.set({
    width:360,
    height:250,
    image_format : 'jpeg',
    jpeg_quality:90
});

function bdino(){
    Webcam.snap(function(a){
        document.getElementById("result").innerHTML='<img id="selfie_image" src="'+a+'"/>';
    });
}
console.log("ml5 version is loaded",ml5.version);
x=ml5.imageClassifier('https://teachablemachine.withgoogle.com/models/gVEBMDobs/model.json',modelloaded);
function modelloaded(){
    console.log("model is loaded");
}





























































































































































































































































































































































































































































